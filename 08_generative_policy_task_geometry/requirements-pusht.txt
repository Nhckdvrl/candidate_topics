# E1 (PushT) stack. Kept separate from requirements.txt so the archived planar-arm
# prototype does not drag the heavy robotics dependencies along.
#
# Reference environment (what the reported results were produced with):
#   conda create -p <env> python=3.11 pip
#   pip install "lerobot[pusht]"
#
# lerobot pins torch<2.11 and pulls gym-pusht, pymunk, pygame, diffusers.
lerobot[pusht]>=0.4.4
numpy>=1.26
pandas>=2.2
scipy>=1.12
scikit-learn>=1.4
matplotlib>=3.8
pytest>=8.0
