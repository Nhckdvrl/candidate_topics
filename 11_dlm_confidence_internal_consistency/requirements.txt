# Topic 11 is intentionally compatible with Topic 10's LLaDA environment.
# Reuse that environment/cache; do not create a second env just for Topic 11.
torch>=2.4
transformers>=4.49,<5
numpy>=1.24
pytest>=8.0
