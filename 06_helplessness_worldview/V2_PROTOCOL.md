# Topic 06 v2 protocol

Status: preregistered final acquisition gate after the Qwen3-8B v1 stop.

The v1 result is not reinterpreted. This v2 changes exactly two independently
motivated items:

1. model: `Qwen/Qwen3-32B`, non-thinking inference;
2. active intervention cost: `2` instead of `1`.

Everything else is frozen: the same four cells, master–yoked pairing, task
families, episode/trial counts, rewards, potential-outcome randomization,
novel `orbital_station` test, primary endpoint, temperature `0.7`, and seed
block `20260821`. No hidden-state probe, self-report, memory summary,
same-family test, prompt redesign, or additional reward manipulation is added.

The first and only v2 stage is a 24-pair-per-diversity S1 (96 sessions) with
40 training interactions and 6 test interactions per session. The hard gate
is acquisition: late-training active intervention and effective-action
selection must separate controllable from yoked-uncontrollable sessions in a
directionally clear way. If `C ≈ U` again, archive Topic 06 without a larger
pilot, model sweep, or further rescue design.

If acquisition is alive, report the frozen novel-test quantities `H1`, `H10`,
and `D = H10 - H1`; do not call the result a parameter-level permanent
worldview. It remains an interaction-history behavioral-prior claim.
