numpy>=2.0
PyYAML>=6.0
pytest>=8.0
torch>=2.7
transformers>=4.51
accelerate>=1.2
einops>=0.8
huggingface-hub>=0.34
# NVIDIA/CUDA validation environment. The full package is required because
# fla-core alone does not include fla.models. For another backend, follow
# upstream FLA INSTALL.md and replace [cuda] with the matching backend extra.
flash-linear-attention[cuda]>=0.5.0
