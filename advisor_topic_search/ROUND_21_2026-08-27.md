# Advisor Topic Search — Round 21 (2026-08-27)

## Objective

Promote the two user-selected advisor candidates to numbered topics and immediately implement the cheapest feasibility-first validation path.

The two selected questions are:

1. meeting-summary decision-state transmutation;
2. normative/deontic semantic drift under text simplification.

Both satisfy the current constitution better than mechanism-first candidates because they begin from external constructs, have public annotation anchors, admit direct behavioral/structural G0s, and naturally leave a method runway.

## Registration

- A3 -> [`29_meeting_decision_state_preservation`](../29_meeting_decision_state_preservation/)
- A4 -> [`30_deontic_semantic_preservation`](../30_deontic_semantic_preservation/)

## Topic29 validation strategy

Public AMI conversion artifacts were inspected before code was written. The documented schema includes ordered dialogue acts plus abstractive `decisions` and summlink links to supporting utterances. The first preflight therefore counts independently annotated decision objects and multi-turn/temporally extended support chains before any model generation.

Implemented:

- deterministic conservative decision-state parser;
- source->summary state-drift scorer;
- AMI artifact-support audit;
- frozen support gates;
- unit tests and preflight runner.

Scientific G0 only starts after the public-artifact support gates pass.

## Topic30 validation strategy

Public LexDeMod artifacts were inspected before code was written. The released classification CSV exposes clause IDs, text, a seven-way deontic label vector, and gold spans. This serves as a measurement anchor independent of simplification outcomes.

Implemented:

- conservative deontic/condition/exception parser;
- LexDeMod support receipt;
- aligned simplification-pair eligibility/drift audit;
- frozen support gates;
- unit tests and preflight runner.

The first corpus-level question is intentionally simple: are there enough normative simplification pairs to support the paper object, and does structured normative drift occur in them?

## Local implementation receipt

Before repository write:

- `pytest`: 4/4 tests passed;
- synthetic AMI fixture exercised the full Topic29 preflight and all four gates;
- synthetic LexDeMod fixture exercised label/span parsing and support gates;
- synthetic simplification-pair fixture exercised eligibility and conditional-drift detection.

These are engineering receipts only; they are not scientific results and are not presented as corpus-level evidence.

## Decision

`REGISTER_29_AND_30 / G0_IMPLEMENTED / RUN_PUBLIC_ARTIFACT_PREFLIGHTS_NEXT`
