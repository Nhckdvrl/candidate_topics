# Advisor Topic Search — Round 15 (2026-08-25)

## Purpose

Reconcile Topic 22 after the completed G0b-v3 scoring-only canonicalization repair.

No new topic is registered.

## Topic 22 final local route result

Commit `285ea8b7530ca24f14b721046efd584be8668499` records the completed v3 rerun.

V3 reused the exact frozen v2 CoT outputs. No behavior was regenerated.

The cached test split exposed 46 distinct canonical pathology labels; the dual-order semantic canonicalizer passed the full 46/46 self-mapping preflight.

V3 resolved 111 previously unresolved branch outputs and reduced pair invalidity:

```text
v2 invalid rate = 0.6250
v3 invalid rate = 0.3242
```

All substantive frozen gates passed:

```text
control accuracy             0.4258  PASS
control-correct              109     PASS
Bias Trap count              43      PASS
Bias Trap Rate               0.3945  PASS
Wilson lower bound           0.3078  PASS
diagnosis transitions        14      PASS
```

But the support gate remained unhealthy:

```text
invalid rate = 0.3242
required <= 0.10
```

Remaining branch-level failures:

```text
control unresolved_final = 64
trap unresolved_final    = 58
```

Final verdict:

```text
MEASUREMENT_CANONICALIZATION_FAILURE
NO_SCIENTIFIC_VERDICT
DIRECT_MODE_NOT_RUN
```

## Correct interpretation

Topic 22 does **not** provide a scientific negative for MedEinst or for the encoding-vs-update distinction.

The correct statement is:

> The released pair structure is clean and the scorable subset shows a strong Bias Trap signal, but the local open-text-to-closed-label measurement interface still leaves too much unusable support to establish the prerequisite population-level behavioral object.

Therefore the **local measurement route** is archived.

## Why the repair sequence stops now

V1 and V2 exposed different concrete defects, so a second repair was legitimate rather than automatically forbidden.

V3 was deliberately narrower than the scientific experiment:

- same frozen CoT outputs;
- no model/sample/seed/threshold changes;
- no ground truth, narrative, or control/trap identity exposed to the mapper;
- explicit abstention;
- dual fixed label orders;
- preflight before rescoring.

It substantially improved support but still missed the frozen ceiling by a large margin.

Any v4 would now require new researcher degrees of freedom on the same observed outcomes: aliases, mapper model choice, prompt/menu design, label-order schemes, semantic thresholds, behavior regeneration, or gate relaxation.

That is no longer a bounded repair of a newly localized defect.

**Rule after Round 15:**

> Measurement repair is defect-based, not mechanically limited to one attempt; however, once the specifically justified bounded repair for the localized defect is run and support remains unhealthy, stop the route rather than optimizing the measurement against the observed sample.

## Direct mode

Direct G0c was preregistered downstream of a healthy CoT prerequisite.

Because G0b-v3 failed support, direct mode was correctly not run and must not be used as post-hoc rescue.

## Mechanism work

No hidden-state, patching, steering, or encoding-vs-update mechanism conclusion is authorized for Topic 22.

## Advisor-pool consequence

Topic 22 leaves the active numbered-work queue and moves to internal archived measurement routes.

This does **not** ban the broad scientific question forever. A future revisit would need a genuinely new external measurement object/evaluation interface whose validity is established independently of these 256 outcomes.

Current advisor search remains focused on:

1. Positional Imprinting — top executable, receipt pending;
2. PK/ICK arbitration history — science top, artifact blocked;
3. Encoding Specificity — active hold;
4. ChronoScope — secondary executable;
5. continued broad search for a stronger mother topic.
